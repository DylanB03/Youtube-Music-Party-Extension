{
  "name": "@ytm-party/backend",
  "private": true,
  "version": "0.1.9",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:e2e": "wrangler dev --config wrangler.e2e.jsonc --port 8797",
    "build": "wrangler deploy --dry-run",
    "types": "wrangler types src/worker-configuration.d.ts",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ytm-party/shared": "file:../../packages/shared"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
